# Local-only reference copies
OLD/

# Git worktrees
.worktrees/

# Brainstorming visual companion
.superpowers/

# Python
backend/.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node / Vite
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

# Coverage report written by `make check-web` (lcov.info + the HTML render).
# Regenerated on every run, so it is a build output rather than a checked-in
# number; CI uploads it as a job artifact instead.
frontend/coverage/

# Build outputs staged by the Makefile (APKs)
/build/

# Run-time pidfiles
.run/
**/results.json
.playwright-mcp/

# Local Codex plugin model override (personal preference, not a repo convention)
.codex/
