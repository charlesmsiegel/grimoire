{#- One shared bond as a line (feeds the # Relationships context block and
    the absorb prompt's relationships head). Vars: a, b (display names),
    bond ({type}). -#}
{{ a }} & {{ b }}: {{ bond.type }}
