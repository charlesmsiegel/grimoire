{#- Vars: card (the V3 card `data` dict). Mirrors tagline/user.j2's shape, but
    reads the speech-bearing fields only -- description/scenario describe the
    person and the situation, which is exactly what an anchor must not be
    about. -#}
Name: {{ card.get("name", "") }}
Personality: {{ card.get("personality", "") or "(none)" }}
Example dialogue: {{ card.get("mes_example", "") or "(none)" }}
Author's speech notes: {{ card.get("system_prompt", "") or "(none)" }}
