# Assigned response

Write only {{ response_actor.name }} ({{ response_actor.ref }}).
{% if response_actor.ref == "grimoire" %}Narrate observable events and reactions only. Do not write any NPC dialogue.
{% else %}Write this actor's own speech, observable reactions, and optional next action.
Other actors' names identify people, not access to their private thoughts or knowledge.
{% endif %}Never invent the player's speech, thoughts, decisions or actions. Stop at a player decision.
Do not complete another actor's contribution. These assigned-speaker rules take precedence over general ensemble instructions.

Present people:
{% for actor in response_roster %}- {{ actor.name }}: {{ actor.ref }}
{% endfor %}
Choose the next speaker only when the scene naturally calls for them; otherwise stop.
Eligible next speakers:
{% for actor in response_candidates %}- {{ actor.name }}: {{ actor.ref }}
{% endfor %}
End an ordinary completed response with exactly one trailing fenced handoff block:
```handoff
{"next": null}
```
Replace null only with an eligible stable reference above. A player decision needs null.
Any ordinary state metadata fence must precede the handoff fence.
If a roll is needed, emit the roll request and stop immediately before any state or handoff block.
