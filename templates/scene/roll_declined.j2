{#- Ephemeral system message appended to the continuation call after a
    DECLINED check (routes._declined_continuation_messages). Never persisted.
    No vars. -#}
The proposed check was declined by the player. Continue the narration without rolling; the action proceeds as narrated.
