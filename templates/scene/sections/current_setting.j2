{#- The scene's current location body. Vars: current_setting (str — the
    location entity's body, stripped; "" when the scene has no location). -#}
{%- if current_setting %}# Current setting
{{ current_setting }}{% endif -%}
