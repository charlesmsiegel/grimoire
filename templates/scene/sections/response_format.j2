{#- The reply-format instruction, always last in the system message.
    Vars: player_names ([str] — seated players the model must never write). -#}
{% if response_actor|default(none) %}Write continuous prose for the assigned speaker. The application supplies their name; omit speaker labels and script headings.
{% else %}Write your reply as a script. Each character who acts or speaks gets their own block starting with **<Name>:** on its own line, e.g. **Seraphine Vale:**. Use **Grimoire:** for narration, scene description, and any voice that isn't a named character.
{% endif %}Enclose spoken dialogue in double quotation marks. Keep actions and narration outside the quotation marks, in ordinary prose.
{%- if player_names %} Never write dialogue or actions for: {{ player_names | join(", ") }}.{% endif -%}
