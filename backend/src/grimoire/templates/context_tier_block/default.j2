{#-
  A retrieved-context tier ("Spotlight", "Background", "Archive").

  Inputs:
    label    str
    items    list[str]  (already prioritized + budget-trimmed)
-#}
# {{ label }}
{{ items | join("\n\n") }}
