<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outbound HTTPS to OpenRouter / chub.ai, plus the loopback server. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.Grimoire"
        android:networkSecurityConfig="@xml/network_security_config"
        android:allowBackup="true"
        android:supportsRtl="true">

        <!-- configChanges: a rotation must not tear down the WebView (and with
             it the SPA's in-memory state); the page is responsive, not the
             activity. Standard practice for WebView shells. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboard|keyboardHidden|smallestScreenSize|uiMode|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Owns the embedded Python server's lifetime; foreground promotion
             during LLM streams is Phase 3 (docs/android-architecture.md). -->
        <service
            android:name=".ServerService"
            android:exported="false" />
    </application>
</manifest>
